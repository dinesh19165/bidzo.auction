package com.bidzo.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface ReportMapper {
    // No backing Report entity exists in the project; mapper kept as a placeholder
}
