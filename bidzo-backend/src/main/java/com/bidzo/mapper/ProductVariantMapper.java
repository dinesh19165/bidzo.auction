package com.bidzo.mapper;

import org.mapstruct.Mapper;

@Mapper(componentModel = "spring")
public interface ProductVariantMapper {
    // No DTOs exist for ProductVariant in the current project; intentionally empty.
}
